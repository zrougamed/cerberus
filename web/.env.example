# Cerberus UI Environment Variables

# Backend API URL (used during development)
# In production, nginx proxies /api/v1/* to the backend
VITE_API_URL=http://localhost:8080

# Optional: Enable mock data mode for development without backend
# VITE_USE_MOCK=true
